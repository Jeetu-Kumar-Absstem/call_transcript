# ABSSTEM ASR V1 — Path A (API Only)
# Install: pip install -r requirements.txt

requests>=2.28.0
soundfile>=0.12.0
numpy>=1.24.0
torch>=2.0.0
torchaudio>=2.0.0
huggingface_hub>=0.20.0
python-dotenv>=1.0.0