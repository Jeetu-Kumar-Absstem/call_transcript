# ABSSTEM ASR V1 — Path A (API Only)
# Install with: pip install -r requirements.txt

# HTTP requests — download WAV + call HuggingFace API
requests>=2.28.0

# Audio preprocessing — lightweight, no GPU needed
torchaudio>=2.0.0
torch>=2.0.0

# HuggingFace API client
huggingface_hub>=0.20.0